# Amoore Specialist Hospital

Static hospital website built with HTML, Tailwind CSS, and JavaScript.

## View locally

Open `code.html` in a browser, or run this in PowerShell:

```powershell
Start-Process .\code.html
```

An internet connection is required for the Tailwind CDN, Google Fonts, and embedded map. No npm installation is needed.
